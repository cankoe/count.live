pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html styles.css script.js logo.png CNAME sitemap.xml robots.txt public/
  artifacts:
    paths:
      - public
  only:
    - main
