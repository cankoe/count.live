name = "count-live"
main = "worker.js"
compatibility_date = "2024-12-01"

# Static assets (your HTML, CSS, JS)
[assets]
directory = "./"
run_worker_first = true  # Run worker BEFORE serving static assets (for OG tag injection)
